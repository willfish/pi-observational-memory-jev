{
  "name": "pi-observational-memory-jev",
  "version": "0.1.0",
  "description": "Observational memory for pi, with Jev as the typed observation backend.",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package", "observational-memory", "jev", "typesafe"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/willfish/pi-observational-memory-jev.git"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
